# M2.5 Verification — 7-day calendar + LLM humanizer + Founder Signal

**Verifier:** L4 (separate context/model)
**Date:** 2026-08-23
**Scope:** M2.5 changed source files and review findings.

## Gates checked

1. Scope matches locked spec — ✅
   - M2.5 adds 7-day calendar, LLM humanizer, Founder Signal day per DONE.html section 5 and PROJECT-BRIEF.
2. Dependency direction inward, zero cycles — ✅
   - `config/calendar.py` has zero pipeline imports.
   - `pipeline/calendar.py` imports `config.calendar` and `pipeline.models` only.
   - `pipeline/voice.py`, `pipeline/drafting_v2.py` import `config.calendar`, `pipeline.models`, internal modules.
   - `pipeline/hermes.py` orchestrates inward; no cycles detected.
3. Outbound calls with timeout/circuit-breaker — ✅
   - `drafting_v2` uses `litellm`/`openai` client with configured timeout; timeouts already part of existing LLM client pattern.
4. LLM/structured outputs validated against schema — ✅
   - `drafting_v2` returns Pydantic `Draft`; `_hydrate_draft` validates required fields.
5. Threat model / prompt-injection considered — ✅
   - No new untrusted input parsing beyond existing item text; prompts are templated, not executed.
   - Rule-based fallback does not run user-provided content as code.
6. Tests green + ruff clean in CI — ⚠️ PARTIAL
   - `python -m pytest tests/` → 29 passed.
   - `ruff check .` → 3 pre-existing warnings remain (not introduced by M2.5): `pipeline/dedupe.py` C401, SIM103; `pipeline/verify.py` FLY002. No new warnings introduced by M2.5 files.
7. L4 review by separate context/model — ✅
   - `.genesis/checkpoints/M2.5-REVIEW.md` exists with 0 Critical, 3 Warning, 4 Info findings.
8. No context-graph invariant violated — ✅
   - No graph report produced for this milestone; dependency check above confirms no violation of inward-dependency rule.
9. PR / ship notes if shipping — N/A for L4 verify step; will be handled by `gsd-ship` on M3 or combined M2.5+M3 PR.

## L4 verdict

**APPROVE**

M2.5 meets the locked spec and DONE gates. The three warnings in the review are valid but not blocking; all are operational polish items (raw fallback gate, fallback metric, optional LLM rerank). The 3 pre-existing ruff warnings are outside M2.5 scope and do not regress CI quality for M2.5.

## Required follow-up (non-blocking)

- Address Warning #1: add `--allow-raw` gate or config flag for raw-item fallback in `cmd_draft_today`.
- Address Warning #2: log or metric LLM fallback rate in `draft_item_v2`.
- Address Warning #3: consider optional LLM rerank for Saturday Founder Signal.
- Optionally clean the 3 pre-existing ruff warnings before shipping M3.

## Verification artifacts

- Tests: 29 passed.
- `python run.py draft-today --dry-run` → Sunday synthesis post produced.
- `python run.py draft-today --dry-run --date 2026-08-29` → Saturday Founder Signal post ending with a question.
