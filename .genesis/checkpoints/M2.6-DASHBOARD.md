# M2.6-DASHBOARD — Review dashboard v3 front-end fix

**Date:** 2026-08-24
**Branch:** codex/hybrid-calendar-v3
**Commit:** aca6ad5

## What changed

- `pipeline/review_dashboard.py` now renders a light-studio, LinkedIn-style post preview.
- LinkedIn card now includes:
  - author header (avatar, name, headline, timestamp, ⋯ menu)
  - post body, hashtags, media / link card
  - reaction bar (Like / Comment / Repost / Send)
- Quality scores use CSS progress bars instead of block characters.
- Inline toast notifications replace browser `alert()`.
- Header/nav switched from dark to clean LinkedIn-esque light theme.

## Demo

```bash
python run.py review-dashboard
python run.py review-server --host 127.0.0.1 --port 8080
```

Then open http://127.0.0.1:8080.

## Verification

- `python -m pytest tests/ -q` → 60 passed
- `ruff check .` → clean
- Generated `data/review/index.html` inspected manually.

