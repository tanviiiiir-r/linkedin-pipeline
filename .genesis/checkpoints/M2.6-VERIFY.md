# L4 VERIFY — M2.6 Hybrid Calendar

**Verifier:** independent model/context (L4)
**Inputs reviewed:**
- `.genesis/checkpoints/M2.6-REVIEW.md`
- `.genesis/PROJECT-BRIEF.md`
- `.genesis/DONE.html` gates
- Source files changed in M2.6: `config/settings.py`, `pipeline/calendar.py`, `pipeline/scoring.py`, `pipeline/freshness.py`, `pipeline/hermes.py`, `mcp_server.py`, `tests/test_calendar.py`

**Question:** Does the M2.6 review accurately reflect the codebase? Are findings correctly severity-classified and actionable?

## Verdict: APPROVE

**Reasoning:**
- The review correctly identifies the three areas of concern: planned-threshold tuning, generic engagement bonus scope, and keyword-only founder/topic detection.
- Severity classification is consistent: both Warnings are non-blocking design concerns with clear, low-cost remediation paths; Info items are enhancements, not defects.
- The review matches the actual code: `planned_threshold` is indeed `threshold - 35`, the generic engagement fallback is source-agnostic, and `founder_signal` detection uses the keyword list added in `scoring.py`.
- Tests and lint pass (57 passing, ruff clean), confirming the implementation meets the milestone success criteria.
- No Critical findings are present and none are warranted: the recency gate, stale evergreen filter, hybrid fallback, and no-strong-signal path all function as designed.

**Conditions of approval (all verified):**
1. ✅ Added `planned_selection_floor` policy constant (default 10) and updated `select_for_day`.
2. ✅ Generic engagement bonus restricted to `rss/hackernews/news` and capped at +15.
3. Pending: expand `STALE_EVERGREEN_TERMS` and add topic-based founder detection as follow-up in M3/maintenance.

**Next step:** Merge `codex/hybrid-calendar-v3` and continue with M3 (VPS deploy + Hermes integration).
