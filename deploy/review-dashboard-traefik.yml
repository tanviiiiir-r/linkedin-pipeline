# Add these labels to a Docker service or use with a standalone TCP target.
# For the systemd approach, Traefik cannot use Docker labels; use the dynamic file below instead.
# Example container labels:
#   traefik.enable=true
#   traefik.http.routers.linkedin-review.rule=Host(`review.hermes-agent-xqcr.srv1921473.hstgr.cloud`)
#   traefik.http.routers.linkedin-review.entrypoints=websecure
#   traefik.http.routers.linkedin-review.tls.certresolver=letsencrypt
#   traefik.http.routers.linkedin-review.middlewares=linkedin-review-auth
#   traefik.http.services.linkedin-review.loadbalancer.server.url=http://172.18.0.1:8080
#   traefik.http.middlewares.linkedin-review-auth.basicauth.users=hermes:$$apr1$$H...hash...
